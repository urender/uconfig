description:
  This section describes all of the services that may be present on the AP.
  Each service is then referenced via its name inside an interface, ssid, ...
type: object
properties:
  ssh:
    $ref: "https://uconfig.org/schema/v1/service/ssh/"
  mdns:
    $ref: "https://uconfig.org/schema/v1/service/mdns/"
  ntp:
    $ref: "https://uconfig.org/schema/v1/service/ntp/"
  log:
    $ref: "https://uconfig.org/schema/v1/service/log/"
